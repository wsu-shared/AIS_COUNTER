# AIS Counter

```
python3 -m aiscounter "/path/to/example-images"
```

Opens the browser reviewer and waits for you to click each AIS, as the original does.
Add `--auto` to have every AIS found for you first, or `--batch` to write reports with no UI.